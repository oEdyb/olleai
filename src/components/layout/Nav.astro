---
const currentPath = Astro.url.pathname;
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-(--color-bg)/90 backdrop-blur-md">
  <div class="max-w-4xl mx-auto px-6 md:px-8">
    <div class="flex items-center justify-between h-16 border-b border-(--color-border)/40">
      <a href="/" class="group">
        <span class="text-base font-medium tracking-wide text-(--color-text) group-hover:text-(--color-accent) transition-colors" style="font-family: var(--font-body);">
          Olle Dyberg
        </span>
      </a>

      <div class="flex items-center gap-6">
        <a
          href="/consulting"
          class:list={[
            "text-base transition-colors",
            currentPath.startsWith("/consulting")
              ? "text-(--color-text)"
              : "text-(--color-muted) hover:text-(--color-text-secondary)",
          ]}
        >
          Consulting
        </a>
        <a
          href="/links"
          class:list={[
            "text-base transition-colors",
            currentPath.startsWith("/links")
              ? "text-(--color-text)"
              : "text-(--color-muted) hover:text-(--color-text-secondary)",
          ]}
        >
          Links
        </a>
      </div>
    </div>
  </div>
</nav>
